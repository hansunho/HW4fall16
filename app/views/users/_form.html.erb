<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field", :id = 'signup_user'>
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id, :id => 'signup_user' %>
  </div>
  <div class="field", :id = 'signup_email'>
    <%= f.label :email%><br>
    <%= f.text_field :email, :id => 'signup_email' %>
  </div>
 
  <div class="actions",:id = 'signup_create'>
    
    <%= f.submit 'Create my account', :id =>'signup_create' %>
    
  </div>
<% end %>
